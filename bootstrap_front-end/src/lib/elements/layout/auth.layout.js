import styled from 'styled-components';
import { THEME_SIZE} from '../../theme'
export const AuthLayout = styled.div`
  width: ${THEME_SIZE.AUTH_LAYOUT.SMALL};
  margin: ${THEME_SIZE.AUTH_LAYOUT.MARGIN};
`;