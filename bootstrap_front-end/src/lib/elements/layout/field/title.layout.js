import styled from 'styled-components';
import { TextSecondary } from '../../text';
import { spacing } from '../../../theme'

export const Title = styled(TextSecondary)`
  margin-bottom: ${spacing(1)};
`;